#include<stdio.h>
int main()
{
	int a[5],i,n=5,elem;
	printf("enter the element");
	for(i=0;i<n;i++)
	{
	scanf("%d",&a[i]);
	}
	 printf("enter the searching element");
	 scanf("%d",&elem);
	 for (int i=0;i<n;i++)
	 {
	 	if(a[i]==elem);
		 {
	 		printf("the element location is %d",i);
		 }
    }
    return 0;    
}
